import React, { Fragment } from 'react'

const Events = () => {

    const handleClick = () => {
        
    }

    return (
        <Fragment>
            <hr />
            <h2>Eventos</h2>
            <button onClick={handleClick}>Click</button>
        </Fragment>
    )
}

export default Events
